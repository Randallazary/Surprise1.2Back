BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Productos] ADD CONSTRAINT [Productos_supplierId_df] DEFAULT 1 FOR [supplierId];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
